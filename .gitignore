# Dependencies
# Ignore the node_modules directories in both frontend and backend
/node_modules
/backend/node_modules
/frontend/node_modules

# Environment Variables
# These files contain sensitive information like API keys and database strings.
# Never commit them to version control.
.env
.env.local
.env.*.local

# Build output
# This is the compiled code generated by Vite for production.
/frontend/dist

# Log files
# These are for local debugging and shouldn't be in the repository.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Operating System / Editor specific files
.DS_Store
.vscode/
.idea/
Thumbs.db